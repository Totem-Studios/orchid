export component MainWindow {
    Window {
        width: 400px;
        height: 300px;
        Text {
            text: "Hello, Slint!";
            horizontal_alignment: center;
            vertical_alignment: center;
        }
    }
}